# Your FREE Hugging Face token (REQUIRED — the model runs on ZeroGPU).
# Create one (no card) at https://huggingface.co/settings/tokens ("Read" is enough).
HF_TOKEN=

# Optional overrides (defaults shown):
# HF_SPACE=ZhengPeng7/BiRefNet_demo
# HF_ENDPOINT=/image
# HF_WEIGHTS=Matting-HR    # BiRefNet variant (e.g. General-HR, Matting, Portrait). "" = a single-image endpoint like /png
# HF_RESOLUTION=           # e.g. 2048x2048; empty = the model's native default
# PORT=8787
